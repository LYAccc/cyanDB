# cyanDB
A database engine based on LSM-trees


1. 1.0 version
   -create database
   -append string data 
   -delete data 
   -"select 1 row" query
2. 2.0 version
   -use hashmap to accelerate query
